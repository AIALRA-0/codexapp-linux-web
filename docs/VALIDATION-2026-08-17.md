# B 站原子首屏与大对话性能验收

日期：2026-08-17

## 结果

B 已发布 `20260817.1445-b2-atomic-first-paint`，对应 Git 提交
`98c321deebdb421e784bc24e5c7cffa07a5d69f8`。A 保持
`20260814.0020-a-resume-notification-order`，没有重启或改配置。

本次修复不替换官方网页。项目、当前任务、最新正文和输入框没有同时就绪前，
官方界面保持在带明确阶段文字的加载页；全部就绪后才一次显示完整界面。

## 正式数据实测

测试对象为约 623 MB 的真实 Trillium Note 任务。自动化只检查预先存在的项目名和
最新正文哨兵，没有修改正式对话。

| 场景                     | 项目名可见 | 最新正文可见 | 完整首屏揭示 |
| ------------------------ | ---------: | -----------: | -----------: |
| 服务冷启动并预热最近任务 |   6.663 秒 |    52.100 秒 |    52.848 秒 |
| 同一运行时热启动         |   5.193 秒 |    10.916 秒 |    11.771 秒 |

两轮的首屏揭示事件都只发生一次，且事件触发前最新正文和输入框已经可见。
两轮均没有页面错误、本地资源失败或白屏，主资源使用 gzip。

冷启动仍需等待官方 App Server 解析数百 MB 历史；本次没有伪造第二套对话数据库，
也没有用不完整缓存提前显示页面。空闲运行时保留时间从 15 分钟调整为 24 小时，
并且只预热最近一个任务，以减少重复冷启动。

## 语言

真实浏览器依次切换并加载以下官方资源：

- English
- 繁體中文（台灣）
- 日本語
- français (France)
- 简体中文

最后保存简体中文。完整刷新后，简体中文、项目和最新正文均保持。

## 代码与发布门槛

- 普通环境：275 项通过，12 项条件跳过
- 官方 26.810 环境：286 项通过，1 项条件跳过
- 官方 preload：21 个方法全部从目标包源码验证
- 格式、lint、TypeScript、源码边界和凭据扫描通过
- 生产依赖与开发依赖审计均为 0 漏洞
- 隔离 Authentik、鉴权隔离、官方 UI 和真实 `turn/start` 通过
- 发布前 B 静止状态快照、逐文件校验和自动回滚元数据通过
- 发布后 A/B 健康，服务重启计数均为 0

## 明确边界

这次解决的是半成品首屏、重复冷启动和不必要的全历史排空，不承诺网络或官方冷解析
变成零等待。麦克风、摄像头、付费操作、账户删除和不可逆删除仍不在无人值守测试范围内。
生产原始截图、用户标识、任务标识和对话正文没有提交到仓库。
